{
  "name": "candle-factory-website",
  "version": "2.0.0-nuxt.0",
  "private": true,
  "description": "彰化重光企業社 - 傳統手工蠟燭工廠網站 (Nuxt 3 SSG)",
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:data": "python scripts/update_products.py",
    "build:all": "pnpm build:data && pnpm generate",
    "deploy": "pnpm build:all && firebase deploy --only hosting"
  },
  "dependencies": {
    "nuxt": "^3.14.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@nuxt/image": "^1.8.0",
    "@nuxtjs/seo": "^2.0.0",
    "@vueuse/nuxt": "^11.0.0",
    "typescript": "^5.6.0"
  }
}
